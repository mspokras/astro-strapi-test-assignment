{
  "name": "universe-test-assignment",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run develop",
    "frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "concurrently": "^6.2.1"
  }
}
